# Model ids are kept in config because OpenRouter availability changes over time.
# The parser accepts YAML-style '- model-id' lines without requiring PyYAML.
models:
  - openai/gpt-4o-mini
  - google/gemini-flash-1.5
  - anthropic/claude-3.5-haiku
  - meta-llama/llama-3.1-70b-instruct
  - mistralai/mistral-small
