"""Optional LLM-assisted candidate generation utilities for LEMON-Factor.

The core LEMON-Factor pipeline remains deterministic. This package only
contains optional helpers for producing candidate predicate decompositions that
must be validated and/or reviewed before they are treated as evidence.
"""
