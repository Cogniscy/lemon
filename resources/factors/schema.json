{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "LEMON-Factor predicate decomposition set",
  "description": "Offline inventory format used by LEMON-Factor. It stores a controlled factor vocabulary and weighted decompositions for graph predicates.",
  "type": "object",
  "required": ["schema_version", "factors", "decompositions", "metadata"]
}
